# Applications

This directory contains runnable applications, including host programs and
independently configured embedded targets. Reusable concepts belong in shared
crates rather than application packages.
