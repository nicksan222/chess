# Applications

This directory contains runnable applications. Reusable concepts belong in shared
crates rather than application packages.

`firmware` is the Raspberry Pi Linux appliance and its product process. The
board carries no microcontroller or bare-metal target; see
[`../docs/architecture.md`](../docs/architecture.md).
