repos:
  - repo: local
    hooks:
      - id: repository-check
        name: Run the package-owned repository commit gate
        entry: just precommit
        language: system
        pass_filenames: false
        always_run: true
