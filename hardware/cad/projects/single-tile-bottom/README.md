# Printable tile tray

`generate.py` owns only the printable `Tile_Bottom_Tray` geometry and its
inspection render. Other projects import
`generated/single-tile-bottom.blend`; they do not recreate the tray.
