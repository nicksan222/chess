# Merged single-tile views

`generate.py` imports the exact generated lid and tray models, adds reusable
wiring references, and produces merged, open, and wired renders. It owns no
printable geometry.
