# Generated assembly renders

`loading.png` shows partial top-down tile installation. `finished.png` shows all
64 separately printed tiles installed in the otherwise empty board tray.
Run `make regen-all` from the repository root to refresh them.
