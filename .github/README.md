# GitHub configuration

This directory owns GitHub-specific collaboration and automation configuration:
issue forms, review ownership, dependency updates, pull request guidance, and CI.
Project behavior remains in package-local `justfile`s rather than workflow YAML.
